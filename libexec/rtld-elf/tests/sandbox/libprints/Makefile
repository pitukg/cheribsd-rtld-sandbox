# $FreeBSD$

.include <bsd.own.mk>

LIB=		prints
SHLIB_MAJOR=	0

LIBDIR=		${TESTSBASE}/libexec/rtld-elf/sandbox
SHLIBDIR=	${TESTSBASE}/libexec/rtld-elf/sandbox

SRCS=		prints.c

LIBADD=		m

.include <bsd.lib.mk>
